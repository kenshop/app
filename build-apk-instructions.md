# راهنمای کامل دریافت فایل APK و نصب اپلیکیشن «پل شیشه‌ای» روی گوشی 📱

برای ساخت و دریافت فایل خروجی **.APK** جهت نصب روی گوشی‌های اندروید، ۳ روش ساده و استاندارد برای شما فراهم شده است:

---

## ⚡ روش اول: ساخت و دانلود آنلاین فایل APK (پیشنهادی و بسیار آسان)

در این پروژه فایل ساخت خودکار گیت‌هاب (`.github/workflows/build-apk.yml`) قرار داده شده است. با این روش نیازی به نصب اندروید استودیو یا نرم‌افزارهای سنگین روی سیستم خود ندارید!

### مراحل:
1. پوشه پروژه را در یک ریپازیتوری **GitHub** آپلود (Push) کنید.
2. وارد صفحه ریپازیتوری گیت‌هاب خود شوید و به تب **Actions** بروید.
3. می‌بینید که اکشن `Build Glass Bridge Android APK` به طور خودکار شروع به کامپایل فایل اندروید کرده است.
4. پس از ۱ الی ۲ دقیقه، روی اجرای انجام شده کلیک کنید و در بخش **Artifacts**، فایل **`Glass-Bridge-Game-APK`** را با ۱ کلیک دانلود کنید!
5. فایل `.apk` دانلود شده را به گوشی خود منتقل کرده و نصب کنید.

---

## 🛠️ روش دوم: ساخت فایل APK روی کامپیوتر شخصی (با Android Studio)

اگر Node.js و Android Studio روی سیستم خود دارید:

1. ترمینال را در پوشه پروژه باز کرده و دستورات زیر را وارد کنید:
```bash
npm install
npx cap add android
npx cap sync android
npx cap open android
```
2. نرم‌افزار **Android Studio** به طور خودکار باز می‌شود.
3. از منوی بالای اندروید استودیو مسیر زیر را دنبال کنید:
   `Build` ➔ `Build Bundle(s) / APK(s)` ➔ `Build APK(s)`
4. پس از چند ثانیه پیغام ساخت موفقیت‌آمیز ظاهر می‌شود و با کلیک روی گزینه **locate** می‌توانید فایل `app-debug.apk` را برداشته و روی گوشی تست کنید!

---

## 📲 روش سوم: نصب مستقیم و آنی وب‌اپلیکیشن (PWA) روی گوشی بدون نیاز به APK

این پروژه مجهز به قابلیت **PWA (Progressive Web App)** و Service Worker آنلاین می‌باشد:

1. لینک بازی یا سایت را در مرورگر **Chrome** یا **Safari** گوشی اندروید خود باز کنید.
2. روی منوی ۳ نقطه مرورگر کلیک کنید.
3. گزینه **«افزودن به صفحه اصلی» (Add to Home Screen)** یا **«نصب اپلیکیشن» (Install App)** را بزنید.
4. آیکون بازی به صورت مستقیم در لیست برنامه های گوشی شما قرار می‌گیرد و به صورت فول‌اسکرین و آفلاین اجرا می‌شود!
